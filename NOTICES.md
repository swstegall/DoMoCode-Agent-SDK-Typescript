# Notices

The SDK runtime is original DoMoCode work and has no runtime dependencies.

Some protocol and policy terminology is informed by the MIT-licensed DoMoCode Swift project
and its documented upstream prior art. Any ported algorithm receives an SPDX header in the
source file where it is used.
